{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "model-options.schema.json",
    "title": "Model options",
    "description": "Defines different options which are used for the model.",
    "type": "object",
    "properties": {
        "batchSize": {
            "comment": "Defines the size of a batch in the training/evaluation process",
            "type": "int",
            "minimum": 1
        },
        "numOfEpochs": {
            "comment": "Defines the number of training epochs.",
            "type": "int",
            "minimum": 1
        }
    }
}