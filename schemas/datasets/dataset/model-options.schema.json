{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "model-options.schema.json",
    "title": "Model options",
    "description": "Defines different options which are used for the model.",
    "type": "object",
    "properties": {
        "batchSize": {
            "description": "Defines the size of a batch in the training/evaluation process",
            "type": "integer",
            "minimum": 1
        },
        "numOfEpochs": {
            "description": "Defines the number of training epochs.",
            "type": "integer",
            "minimum": 1
        },
        "numReturnSequences": {
            "description": "Sets the number of expected return sequences to predict different suggestions",
            "type": "integer",
            "minimum": 1
        },
        "maxSequenceLength": {
            "description": "Sets the maximum length of the predicted sequence",
            "type": "integer",
            "minimum": 1
        },
        "useContextTypes": {
            "description": "If true, the types available in a file are also sent to the predictor and used for training.",
            "type": "boolean"
        },
        "useTypePrefixing": {
            "description": "Adds a prefix to any types in the sequences on training.",
            "type": "boolean"
        },
        "generationTasks": {
            "description": "Defines further options for method generation tasks.",
            "type": "object",
            "properties": {
                "parameterNames": {
                    "description": "Defines, which tasks should also be performed when generating parameter names in the same task.",
                    "type": "object",
                    "properties": {
                        "withReturnType": {
                            "description": "If true, the parameter list generation will be extended by return type generation in the same task.",
                            "type": "boolean"
                        },
                        "withParameterTypes": {
                            "description": "If true, the parameter list generation will be extended by parameter type generation in the same task.",
                            "type": "boolean"
                        }
                    }
                },
                "parameterTypes": {
                    "description": "If true, parameter type generation is performed in a separate task.",
                    "type": "boolean"
                },
                "returnType": {
                    "description": "If true, return type generation is performed in a separate task.",
                    "type": "boolean"
                }
            }
        },
        "emptyParameterListByKeyword": {
            "description": "If true, a keyword (like 'void') is used to indicate no output.",
            "type": "boolean"
        },
        "adafactor": {
            "description": "Settings for adafactor optimizer",
            "type": "object",
            "$ref": "adafactor.schema.json"
        },
        "modelName": {
            "description": "Defines, which model should be used for the given task.",
            "type": "string",
            "enum": ["t5", "bart"]
        }
    }
}